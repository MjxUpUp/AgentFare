{
  "name": "@agentfare/langchain",
  "version": "0.1.1",
  "private": true,
  "license": "MIT",
  "description": "LangChain callback handler integration (@alpha — all methods are stubs, not yet functional)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8",
    "vitest": "^3.2"
  }
}
