node_modules/
dist/
*.tsbuildinfo
.turbo/
coverage/
agentfare-data/
*.db
docs/

# pnpm project — npm's lockfile is not used; only pnpm-lock.yaml is canonical
package-lock.json
e2e/package-lock.json

# Forge runtime artifacts (task state, reviews, sessions, hooks) — not source
.forge/

# Local IDE / agent tooling & scratch (not source)
.ccg/
.cursor/
.dev-workbench/
.playwright-mcp/
.research/
prototype/
mcp-servers.toml
